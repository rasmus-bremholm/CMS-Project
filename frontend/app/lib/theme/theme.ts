"use client";

import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
	palette: {
		primary: {
			main: "#1a1a1a",
		},
		background: {
			default: "#fafafa",
		},
	},
	typography: {
		fontFamily: "inherit",
	},
});
