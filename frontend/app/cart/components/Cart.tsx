"use client";

import { Box } from "@mui/material";

export default function Cart() {
  return (
    <Box
      display="flex"
      gap={2}
      py={2}
      borderBottom={1}
      borderColor="divider"
      alignItems="center"
    ></Box>
  );
}
